#include "monty.h"
/**
 * stacks - Prints the elements in the stack.
 *
 * This function prints all the elements in the stack
 *
 * @head: A pointer to the head of the stack.
 * @counter: The line number in the code where this operation is executed.
 *
 * Return: This function does not return any value.
 */
void stacks(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
